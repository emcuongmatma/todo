import 'package:flutter/cupertino.dart';

class FocuseScreen extends StatelessWidget {
  const FocuseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text("FocuseScreen"),
        )
      ],
    );
  }
}
